# Basic Tools needed
- Git   `git --version`
- Node.js LTS   `node --version` or `node -v`
- npm   `npm -v`
- python 3.14 (recommended)  `python3 --version`
- pip   `pip3 --version`
- Docker    `docker --version`
